# Vite environment variables (must be prefixed VITE_ to be exposed to the browser)
# Set this to your Railway backend URL in production (no trailing slash)
VITE_API_URL=https://your-backend-service.up.railway.app
